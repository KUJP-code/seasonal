-# locals: (resource:)

- if resource.errors.any?
  .errors.text-bg-danger.p-2.rounded
    %h3= t('.unable_to_save', model: resource.class.model_name.human)
    %ul
      - resource.errors.full_messages.each do |msg|
        %li= msg
