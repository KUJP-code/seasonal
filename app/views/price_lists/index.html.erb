<main id="price_list_index" class="d-flex flex-column gap-3">
    <div class="d-flex gap-3">
        <%= link_to t('.create_price_list'), new_price_list_path, class: 'btn btn-primary' %>
    </div>

    <%= render partial: 'price_lists/price_list', collection: @price_lists %>
</main>