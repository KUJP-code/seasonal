<% if @invoice.seen_at.present? &&
      (@invoice.versions.empty? ||
      @invoice.seen_at >= @invoice.versions.last.created_at) %>
    <%= turbo_stream.replace "child#{@invoice.child_id}changes" do %>
        <td rowspan="2"></td>
    <% end %>
<% end %>