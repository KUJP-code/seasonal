<%= line_chart @coupons.group_by_day(:created_at).count,
               title: t(".daily_coupon_use"),
               height: "50vh" %>

<%= pie_chart @coupons
              .where.not(code: "")
              .group(:code).count,
              title: t(".coupon_popularity"),
              height: "50vh" %>